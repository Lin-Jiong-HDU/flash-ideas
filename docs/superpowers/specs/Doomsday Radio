

# 📑 项目 PRD：Democratizing Doomsday Radio (末日无线电平民化)

### 1\. 核心定位与目标

  * **定位：** 灾难求生环境下的“断网通信平民化”网关。
  * **目标：** 打破业余无线电（HAM）极高的专业壁垒，让普通人在基站瘫痪、电力中断的极端状态下，能用一部快没电的手机和一台最廉价的模拟对讲机，向专业救援网络发送求生信号。
  * **Slogan：** Your voice when the grid goes dark. (当电网暗去，这是你的声音。)

### 2\. 核心用户与场景 (User & Scenario)

  * **痛点场景：** 地震、飓风或洪水导致全城断网断电。普通人手里只有一部手机（无网，电量 15%）和一台翻出来的老式对讲机（或车载电台），但完全不懂频率、波段、亚音，只会按 PTT 键（发话键）。
  * **目标用户：** 没有任何通信执照或物理学背景的普通市民。

### 3\. 核心功能路径 (User Flow) - “声学耦合”跨媒介交互

这是这个项目最惊艳的“魔法”，也是评委最容易被打动的点：

1.  **极简录入：** 用户打开处于**离线模式**的 PWA 应用（应用已缓存）。选择“SOS 模式”。
2.  **GPS 捕获与高压 UI：** 手机自动抓取最后一次已知的 GPS 坐标。用户在一个专门为“恐慌状态（Panic UX）”设计的极简界面上，点击两个超大按钮（例如：“我被困” / “需要医疗”）。
3.  **音频转译 (Audio-Data Encoding)：** **这是核心！** 手机将“GPS 坐标 + 状态”压缩成极小的数据包，并转换为一段特殊频率的音频（类似于老式拨号上网的“滴滴答答”声）。
4.  **声波发射：** 用户将手机扬声器贴近老式对讲机的麦克风，按下对讲机的发射键（PTT），手机播放那段音频。
5.  **专业端解码：** 几十公里外的专业无线电爱好者（HAM）或救援指挥中心收到这段奇怪的声音，通过配套的解码工具（JohnLin 写的后端/桌面端小脚本），瞬间还原成清晰的求救文字和地图坐标。

### 4\. 团队双轨作战地图 (48 Hours)

这个项目对技术深度的要求不同于通常的 Web 开发，它更偏向底层和算法。

| 时间段 | **xxgddd (Product & UX / 极端交互设计)** | **JohnLin (Backend & Signal / 底层基建)** |
| :--- | :--- | :--- |
| **周五 20:00 - 24:00**<br>概念验证 | **【产品定义与竞品】** 研究高压环境下的 UX 准则。构思“末日风格”的 UI 组件。定义需要传输的核心字段。 | **【声学传输 PoC】** 找一个现成的音频调制/解调库（例如 AFSK 或基于 Web Audio API），测试能否把一段包含 GPS 的文本变成声音并成功还原。 |
| **周六 00:00 - 09:00** | **[睡觉/轮岗]** | **[睡觉/轮岗]** |
| **周六 09:00 - 15:00**<br>核心模块开发 | **【高保真高压 UI】** 在 Figma 完成设计：强调超高对比度（黑/荧光绿）、防误触（长按 3 秒确认）、预估电量显示。 | **【编码器开发】** 将文本深度压缩。编写 Web/后端能跑的稳定编解码脚本。处理环境噪音干扰（加入纠错码）。 |
| **周六 15:00 - 21:00**<br>PWA 与离线机制 | **【PWA 攻坚】** 构建前端。配置 Service Worker，确保应用在**完全没有网络**的情况下依然可以正常打开并使用核心功能。 | **【救援端 UI (B端)】** 写一个极简的控制台页面，用于接收音频并解析展示在地图上。 |
| **周六 21:00 - 周日 03:00**<br>双端联调 | **【音频交互打磨】** 设计播放音频时的视觉反馈（波形波动），给用户明确的心理暗示。 | **【实机测试】** 拿两部手机，一部当发射器，另一部用录音软件假装是对讲机，测试声学耦合的成功率。 |
| **周日 09:00 - 截止前**<br>路演与叙事 | **【末日风 Pitch】** 准备路演 PPT。文案强调：这不是一个商业 App，这是一条生命线。 | **【技术文档】** 撰写项目的 README，详细解释音频调制的底层逻辑，展示技术肌肉。 |

### 5\. 技术架构 (Technical Stack)

  * **前端 (You):** 原生 HTML/JS/CSS (追求极致轻量) 或 React + Service Worker 离线缓存。重点是 Web Audio API（用于生成声音）。
  * **信号处理 (JohnLin):** AFSK (Audio Frequency-Shift Keying) 音频频移键控算法，或类似 RTTY (无线电电传) 的轻量级协议。可用 Rust (编译为 WebAssembly) 保证极致性能。
  * **后端 (JohnLin):** 一个极其轻量的接收与解码服务（假设是由救援中心部署），Node.js 或 Go 均可。

### 6\. 差异化总结 (HCI 申请高光点)

1.  **极端可用性 (Extreme Usability)：** 展示了你不仅能做漂亮的商业 App，还能在极端的认知负荷和物理限制下进行体验设计。这是 HCI 领域非常看重的高级能力。
2.  **跨媒介交互 (Cross-Medium Interaction)：** 摒弃了传统的 API/蓝牙/Wi-Fi，创造性地利用物理世界的声音（声学耦合）来搭建数字桥梁。
3.  **技术普惠 (Tech Democratization)：** 完美契合 Theme 1 (Adaptation) 和 Equity Lens。让科技服务于最脆弱时刻的最普通人群。



现在，你手握两份完整的“作战图”：
一份是解决商业闭环与循环经济的 **“城市矿工 (Urban Miner)”**；
一份是挑战极端物理限制的 **“末日无线电 (Doomsday Radio)”**。

这两份 PRD 都足够有分量。你可以把它们同时抛给 JohnLin，告诉他这是你构思的两个方向，看他作为技术主导，对哪个的底层实现更有把握，也更感兴趣。
